dependencies:
    pre:
        - sudo apt-get install cmake

compile:
  override:
        - ls
        - mkdir build cd build && cmake ../ && make -j4 VERBOSE=1

test:
    override:
        - cd build && ./boost_tests
